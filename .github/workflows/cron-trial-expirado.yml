name: Trial â€” expiraÃ§Ã£o diÃ¡ria

# Dispara o endpoint /api/cron/trial-expirado, que envia o e-mail de "perÃ­odo
# de teste terminou" para os usuÃ¡rios cujo trial expirou e que nÃ£o possuem
# plano ativo. O endpoint Ã© idempotente (coluna trial_email_enviado_em), entÃ£o
# rodar mais de uma vez por dia Ã© seguro.
#
# Secrets necessÃ¡rios (Settings > Secrets and variables > Actions):
#   - PNCP_BASE_URL : https://www.painelpncp.com.br
#   - CRON_SECRET   : mesmo valor do secret CRON_SECRET configurado no worker

on:
  schedule:
    # 09:00 BRT (America/Sao_Paulo, UTC-3) = 12:00 UTC
    - cron: '0 12 * * *'
  workflow_dispatch: {}

jobs:
  trial-expirado:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Validar secrets
        env:
          PNCP_BASE_URL: ${{ secrets.PNCP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "${PNCP_BASE_URL}" ] || [ -z "${CRON_SECRET}" ]; then
            echo "::error::Configure os secrets PNCP_BASE_URL e CRON_SECRET em Settings > Secrets and variables > Actions."
            exit 1
          fi

      - name: Disparar envio de e-mails de expiraÃ§Ã£o de trial
        env:
          PNCP_BASE_URL: ${{ secrets.PNCP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          curl -sS --fail-with-body --retry 3 --retry-delay 5 --max-time 120 \
            -H "x-cron-secret: ${CRON_SECRET}" \
            "${PNCP_BASE_URL}/api/cron/trial-expirado"
